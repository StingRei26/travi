// PostCSS config — Tailwind removed to avoid lightningcss platform dependency.
// App uses inline styles; postcss runs with no extra plugins.
const config = {
  plugins: {},
};

export default config;
